{
  "name": "git-gosling",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "prettier": "prettier --cache --check .",
    "prettier-fix": "prettier --cache --write .",
    "prepare": "husky install"
  },
  "devDependencies": {
    "husky": "8.0.3",
    "prettier": "3.2.1"
  }
}
