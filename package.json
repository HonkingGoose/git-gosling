{
  "name": "git-gosling",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "prettier": "prettier --cache --check .",
    "prettier-fix": "prettier --cache --write .",
    "prepare": "husky"
  },
  "devDependencies": {
    "husky": "9.0.10",
    "prettier": "3.2.4"
  }
}
