{
  "name": "git-gosling",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "prettier": "prettier --cache --check .",
    "prettier-fix": "prettier --cache --write .",
    "prepare": "husky"
  },
  "devDependencies": {
    "husky": "9.1.7",
    "prettier": "3.8.1"
  }
}
